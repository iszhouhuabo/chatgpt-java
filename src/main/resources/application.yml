server:
  port: 8080

# DataSource Config
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${DB_HOST:localhost}:3306/tl?serverTimezone=UTC&useSSL=false&allowPublicKeyRetrieval=true
    #    url: jdbc:mysql://43.153.24.43:3306/tl?useAffectedRows=true&allowMultiQueries=true&characterEncoding=utf8&useUnicode=true&useSSL=false&serverTimezone=Asia/Shanghai&autoReconnect=true&failOverReadOnly=false&maxReconnects=10
    username: root
    password: root
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
#  sql:
#    init:
#      schema-locations: classpath:db/schema-h2.sql
#      data-locations: classpath:db/data-h2.sql
#      mode: always


# chatgpt 设置
chatgpt:
  # 默认的Token长度
  max_tokens: ${MAX_TOKENS:512}
  # 尝试将值设为 0.9 以获得更有创意的回复，将值设为 0 以获得具有明确答案的回复
  temperature: ${TEMPERATURE:0.6}
  # 默认的AI模型，gpt-3.5-turbo or gpt-3.5-turbo-0301	，区别：https://platform.openai.com/docs/models/gpt-3-5
  model: gpt-3.5-turbo
  # 多个Key用,分割
  api_key: ${API_KEY:}
  # 默认主机，如果代理的话，就配置代理的主机
  api_host: https://api.openai.com/
  # 是否使用内置 key
  use_internal_key: ${USE_INTERNAL_KEY:true}
  # 训练文件要存储的地方
  train_file: ${TRAIN_FILE:/Users/zhouhuabo/tmp/}